import { authHandler as handler } from "@/server/resources/auth/auth.contract";

export {
  handler as GET,
  handler as POST,
  handler as PUT,
  handler as PATCH,
  handler as DELETE,
  handler as OPTIONS,
};
